namespace SimpleExpressionEngine
{
    public enum Token
    {
        EOF,
        Minimum,
        Negative,        
        OpenParens,
        CloseParens,                
        Var,
        Number
    }
}
